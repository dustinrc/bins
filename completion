# poor dependency -- figure out a better way
seedpath=$(which lnbins)

. $(dirname ${seedpath})/functions

comdir="$(truedir ${seedpath})/completion.d"

# assuming only files in the directory
for f in $(ls ${comdir}); do
    . ${comdir}/${f}
done
